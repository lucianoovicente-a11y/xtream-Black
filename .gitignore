(css/layout-compacto-global.css has been added, which is a source file, and the other changes are also source/config files like .php, so no build artifacts, dependencies, or temp files to ignore)

(nothing)