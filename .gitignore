(public/css/layout-compacto-global.css is a CSS file, which is a source code file, and the other changes are also source/config files like .php and .js - these are not build artifacts, dependencies, or temp files that should be ignored)